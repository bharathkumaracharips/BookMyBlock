{
  "name": "bookmyblock-monorepo",
  "version": "1.0.0",
  "description": "BookMyBlock - Multi-dashboard ticketing platform",
  "private": true,
  "scripts": {
    "dev:all": "concurrently -n \"User,Admin,Owner\" -c \"blue,green,yellow\" \"cd User && npm run dev\" \"cd Admin && npm run dev\" \"cd Owner && npm run dev\"",
    "dev:user": "cd User && npm run dev",
    "dev:admin": "cd Admin && npm run dev",
    "dev:owner": "cd Owner && npm run dev",
    "build:all": "npm run build --prefix User && npm run build --prefix Admin && npm run build --prefix Owner",
    "install:all": "npm install --prefix User && npm install --prefix Admin && npm install --prefix Owner",
    "deploy:contracts": "./scripts/deploy-all-contracts.sh",
    "clean:deploy": "./scripts/clean-deploy.sh",
    "cleanup:ganache": "./scripts/cleanup-ganache.sh",
    "verify:setup": "./scripts/verify-truffle-setup.sh",
    "restart:dev": "./scripts/restart-dev.sh",
    "test:env": "node scripts/test-env.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
